# D1 后续平台验证

2026-09-15，用户接受当前本机交付并要求归档。原 D1 A7 从本次归档阻塞移为后续待办；A1-A6 的实现与通过证据保持不变。此调整不代表双平台已经实测通过。

| 目标环境 | 当前状态 | 后续需要的证据 |
| --- | --- | --- |
| Windows 10 1809+ x64 | NOT RUN | 准确系统版本、MSVC/Qt/客户端库版本；真实窗口启动、本地 React/桥接、QMYSQL 加载、最小连接成功/失败、Credential Manager、导出及缩放 |
| macOS 15 arm64 | NOT RUN | 准确系统版本、Qt/客户端库版本；真实窗口启动、本地 React/桥接、QMYSQL 加载、最小连接成功/失败、Keychain、导出及缩放 |

现有 macOS 27 arm64 实测与 `minos 15.0` 检查不能替代上述证据。本机 SDK 27 的兼容限制继续保留。

取得目标系统环境后另行补验；在对外声明目标平台已验证或正式发行前完成。没有有效证据时继续标 NOT RUN，不自动升级为通过。详见 [D1 检查记录](d1-validation.md)。
